# Photos

Ambient mode reads photos you add through **Settings → Photos**, which stores
them in the iPad's own IndexedDB — nothing is uploaded and nothing is committed
to the repo.

This folder is just a placeholder. If you would rather serve a fixed set of
photos with the site, drop image files here and reference them as remote URLs
in `state.ambient.urls`.
